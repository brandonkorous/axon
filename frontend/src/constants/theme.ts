/**
 * Default agent color — used as fallback when an agent has no ui.color set.
 * Matches the primary token in src/theme.css (axon light theme).
 *
 * oklch(64% 0.06 190) ≈ #7A9E9E
 */
export const DEFAULT_AGENT_COLOR = "#7A9E9E";
