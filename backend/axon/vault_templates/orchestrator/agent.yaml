id: "{{AGENT_ID}}"
name: "{{AGENT_NAME}}"
title: "{{AGENT_TITLE}}"
tagline: "{{AGENT_TAGLINE}}"
type: orchestrator

model:
  max_tokens: 4096
  temperature: 0.7

vault:
  root_file: second-brain.md
  writable_paths: ["."]

delegation:
  can_delegate_to: []
  accepts_from: []

behavior:
  auto_save: true
  first_message: true

learning:
  enabled: false

ui:
  color: "#8B5CF6"
  sparkle_color: "#A78BFA"
